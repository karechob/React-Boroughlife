import MainLayout from "../Layout/MainLayout"

function EventsPage() {
  return (
    <MainLayout> 
      <div style={{height: 700, fontWeight: 'bolder', fontSize: 30, color: 'red', marginInline: '800px'}}>Events Page</div>
    </MainLayout>
  )
}

export default EventsPage 